---
name: localize-website-zh
description: End-to-end localization of complete websites into professional Simplified Chinese, from authorized source capture and localization planning through parallel subagent translation, global bilingual review, runtime UI verification, and Git-ready delivery. Use when Codex is asked to fully translate, 汉化, 中文化, mirror, clone, or localize a static site, documentation site, interactive educational site, SPA, or supplied web source while preserving structure, behavior, assets, formulas, and dynamic user-facing text.
---

# 完整网站中文化

把网站中文化视为内容工程，而不是批量替换字符串。最终交付达到高级专业人工翻译以上的准确性、完整性、自然度和领域可信度，并保持原站对最终用户可见的结构与行为。

## 不可妥协的交付约束

- 将未经修改的完整基准放在 `origin/`，将中文版本放在 `zh/`。
- 保持 `origin/` 只读；所有翻译和本地化修改只进入 `zh/`。
- 持续维护过程文档，而不是完工后补写。
- 完整覆盖静态正文、动态脚本文案、菜单、弹窗、图注、表单、错误状态、辅助文本及运行时生成内容。
- 保留公式、代码、变量、占位符、DOM 标识、URL、数据键和程序控制结构，除非本地化确实要求修改。
- 先完成全部初译，再统一检查、润色和优化；不要让翻译阶段演变为零散的全局审查。
- 只在最终阶段集中进行完整验证；前期只做能解除阻塞的轻量检查。
- 不把盲目的逐字符串机器翻译当作最终译文。使用上下文、领域知识和双语对照完成翻译；遵守用户对工具和翻译方式的限制。

## 开始工作

1. 阅读 [capture-and-inventory.md](references/capture-and-inventory.md)，规划抓取与内容表面清单。
2. 阅读 [translation-quality.md](references/translation-quality.md)，制定质量、术语和并行翻译规则。
3. 阅读 [repository-delivery.md](references/repository-delivery.md)，确定仓库、构建与交付边界。
4. 对新项目运行 `scripts/scaffold_localization_repo.py <project-root>`，生成标准目录和过程文档；已有项目则按需补齐，绝不覆盖现有文件。
5. 建立四阶段工作计划。任何时刻只推进当前阶段，除非发现会使当前工作失效的阻塞问题。

## 第一阶段：完整抓取网站

### 确定基准

- 明确入口 URL、允许范围、目标语言区域、是否需要登录、是否有用户提供的本地源码，以及最终交付是否必须完全离线。
- 优先把用户提供的源码作为基准；远程网站用于补充运行时证据和遗漏资源。
- 记录抓取日期、入口、工具、重定向、外部依赖、已知源站错误和不可静态化的后端边界。
- 区分网站自身内容与浏览器扩展、翻译插件、调试工具、广告注入或个人会话产生的标记。

### 捕获完整表面

- 抓取所有目标路由及 HTML、CSS、JavaScript、图片、字体、图标、媒体、清单、Worker、WASM、局部数据文件和动态分块。
- 通过运行时网络记录补充懒加载图片、CSS 字体、动态导入、Canvas/SVG 资源、交互后出现的资源和脚本构造的 URL。
- 对 SPA、SSR、静态生成站点和文档生成器分别识别其真实入口和构建边界；不要假定首页抓取等于完整网站。
- 生成路由与资产清单，确认本地引用关系。允许源站已有失败，但必须记录并区分。

### 固化原文

- 将完整基准放入 `origin/`，建立 `docs/SOURCE_MANIFEST.md`。
- 从 `origin/` 创建一次 `zh/` 工作副本；此后禁止以重新复制覆盖翻译进度。
- 在开始翻译前记录文件数量、主要类型、入口页、外部依赖和不可复现内容。

## 第二阶段：确定翻译策略

在任何批量翻译前完成：

- `docs/LOCALIZATION_BRIEF.md`：受众、用途、简体中文区域、语气、专业深度、品牌与人名策略。
- `docs/GLOSSARY.md`：推荐译法、禁用译法、语境规则、专有名词和保留原文项。
- `docs/STYLE_GUIDE.md`：标点、空格、数字、单位、标题、按钮、第二人称、技术名词和中英文混排规则。
- `docs/PROGRESS.md`：可并行工作单元、文件所有权、状态和阻塞项。
- `docs/DECISIONS.md`：对歧义、范围变化、依赖处理和不可逆判断留下理由。

先扫描并分类所有用户可见文案：

- HTML 文本节点和元数据；
- `title`、`alt`、`placeholder`、`aria-*`、SVG 文本；
- JavaScript/JSON 中的 UI 字符串、字典、模板和状态文案；
- 弹窗、菜单、Toast、加载、空状态、错误和帮助内容；
- Canvas 图示标签、图例、时间轴、字幕和动态生成文本；
- 第三方组件中最终用户确实会看到的本地化界面。

建立受保护内容清单。对模板占位符、插值表达式、转义序列、Markdown/HTML 结构、代码、公式、键名和数据绑定采取最小改动。

## 第三阶段：多 subagent 并行初译

当网站具有至少两个有实际工作量的独立翻译单元且并发能力可用时，必须使用多个 subagent；主 agent 也应承担一个互不重叠的翻译工作单元。不要为了满足形式上的并行要求，把极小网站拆成大量微任务。若平台并发槽不足，保留工作单元边界并顺序执行，同时在进度文档中记录原因。

### 划分工作

- 按章节、路由、功能模块或独立文件树拆分，保证文件所有权不重叠。
- 单独分配共享脚本、全局菜单、动态字典和第三方可见界面，避免它们落在章节责任之外。
- 所有翻译者必须先阅读 Brief、Glossary 和 Style Guide。
- 为每个工作单元提供原文路径、中文路径、允许修改范围和进度记录路径。

### 约束翻译阶段

- 只翻译分配范围；不进行全站审计、依赖裁剪、构建重写、样式重构或邻近代码清理。
- 以段落、论证和交互语境为单位翻译，不孤立处理单句或词条。
- 遇到术语冲突或绑定关系不明时，记录问题；不要静默创造第二套译法。
- 保持文件语法和受保护标记完整，但不在本阶段反复运行全站验证。
- 每个 subagent 写入独立的 `docs/progress/<workstream>.md`，由主 agent 汇总状态，避免并发修改同一进度文件。

只有当所有计划内文案表面均完成初译、所有工作单元均交回、未决问题已集中记录时，才能进入下一阶段。

## 第四阶段：整体检查、润色与优化

按以下顺序集中完成，不要边查边无差别重写：

1. **完整性审计**：核对路由和文案表面清单，找出遗漏英文、未翻译属性、动态字符串和运行时条件分支。
2. **双语准确性审计**：逐段对照原文，检查增译、漏译、错译、指代、逻辑关系、数字、否定和专业含义。
3. **术语与文风审计**：统一词汇、语气、标题层级和中文表达，消除翻译腔，同时保留原文信息密度。
4. **动态语义审计**：追踪“交互事件 → 状态/索引 → 文案键 → 用户界面”，防止文案正确但显示场景错配。
5. **界面本地化审计**：检查中文排版、换行、按钮长度、表格、图例、Canvas 标签、ARIA 和响应式布局。
6. **功能与资源审计**：在本地运行中文站，检查入口、导航、公式、图片、字体、媒体、交互、Worker、404 和控制台错误。
7. **仓库审计**：清除临时文件，确认构建可复现、文档完整、Git 状态可解释。

对大型网站，可把最终审计按“专业内容与文风”“术语与遗漏”“动态 UI 语义”“技术与资源完整性”分给独立 reviewer subagent；由主 agent统一裁决并实施修改。

## 专业翻译质量门槛

最终译文必须同时满足：

- **信**：事实、逻辑、技术含义、数字、条件和语气准确；
- **达**：中文自然、清晰、无歧义，读者无需反译回原文；
- **雅**：符合目标领域与受众的成熟文风，避免生硬直译和口号化润色；
- **一致**：术语、专名、格式、交互指令和称谓前后一致；
- **完整**：所有最终用户可见状态均被覆盖；
- **可用**：中文化不破坏结构、行为、可访问性或部署能力。

领域内容必须由具备该领域表达能力的 reviewer 进行最终通读。遇到不确定术语时查证权威材料并记录选择理由，不凭词典第一义机械翻译。

## 过程文档纪律

- 每完成一个阶段就更新相应文档和 `PROGRESS.md`。
- 记录事实、推断、未决问题和已接受限制；不要把未验证假设写成结论。
- 重大术语、范围和构建决策写入 `DECISIONS.md`。
- 最终将验证范围、结果、已知限制和交付文件写入 `QA_REPORT.md`。
- README 只在目录和构建方式稳定后定稿，说明来源、使用方式、项目结构、翻译方法和免责声明。

## Git 与构建交付

最终仓库至少包含：

```text
origin/                   # 未修改的原文基准
zh/                       # 完整中文版本
docs/                     # 过程与质量文档
README.md
.gitignore
```

只有确有需要时才加入构建脚本或 Makefile：

- `zh/` 可直接静态部署时，不制造额外构建层。
- 大型第三方依赖、生成文件或平台专用发布树可以由脚本重建，但必须固定版本和校验依据。
- 构建产物、缓存和临时抓取目录默认不提交；最终用户无法重建或属于翻译成果的文件必须提交。
- 构建不得依赖个人绝对路径、隐式登录状态或本地专属工具。
- 除非用户明确要求，不推送远程仓库、不创建提交，也不把部署状态当作源码。

从干净的仓库状态执行最终构建或直接运行 `zh/`，完成一次集中验证，再交付文件清单、验证结论和已知限制。

交付前必须收口：

- 将 `PROGRESS.md` 的阶段和工作单元更新为最终状态；
- 用真实验证结果完整填写 `QA_REPORT.md`，删除模板占位内容；
- 定稿 README 和 `.gitignore`；
- 若目标目录尚不是 Git 仓库，执行本地 `git init`；除非用户明确要求，不提交或推送；
- 检查 `git status`，确保每个未跟踪或忽略项都有明确理由。

## 资源

- [capture-and-inventory.md](references/capture-and-inventory.md)：抓取静态站、SPA、动态资源和运行时文案表面的详细方法。
- [translation-quality.md](references/translation-quality.md)：专业翻译、术语、subagent 分工和最终审校方法。
- [repository-delivery.md](references/repository-delivery.md)：`origin/`、`zh/`、过程文档、构建脚本和 Git 交付规范。
- `assets/project-docs/`：过程文档模板，由脚手架脚本复制到目标仓库。
- `scripts/scaffold_localization_repo.py`：安全创建标准目录和缺失模板，不覆盖已有内容。
